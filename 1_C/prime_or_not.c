#include <conio.h>
#include <stdio.h>

void main()
{
    int n = 5;// Assigin the value 
    int k = 6;
    if (n == k) // Campare the two values.
    {
        printf("it's same");
    }
    else
    {
        printf("not same");
    }

    /* int n;
       scanf("%d",&n);
    if(n%2==0){
        printf("isPrime");
    }
    else {
        printf("Not Prime");
    }
    return;
    */
}